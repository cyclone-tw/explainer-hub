# GitHub Pages

This site is served from the `/docs` folder on the default branch.

After the first push, enable Pages in repo settings if automation did not:

1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main` / folder: `/docs`

Hub URL: https://cyclone-tw.github.io/github-analyzation/
